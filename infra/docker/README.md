# Container infrastructure

The root `Dockerfile` and `docker-compose.yml` define the initial local environment.
Redis is intentionally absent until a demonstrated shared/ephemeral-state use case exists.

