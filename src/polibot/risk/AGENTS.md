# Risk subsystem rules

- Fail closed on stale, missing, ambiguous, or unhealthy state.
- Decisions must be deterministic with machine-readable rejection reasons.
- Never add an execution bypass or use binary floating point for financial values.
- Cover every new approval condition with rejection tests.

